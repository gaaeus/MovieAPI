This is a Visual Studio 2019 project.
ASP .NET Core 3.1 is being used.

Run the project and use the endpoints as:

https://<yourserver>:<port>/metadata
https://<yourserver>:<port>/metadata/<id>
https://<yourserver>:<port>/movies/stats
